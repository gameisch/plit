<%= form_for current_user.relationships.find_by_followed_id(@user),
             :html => { :method => :delete } do |f| %>
  <div title="перестать следить за его новостями" class="actions"><%= f.submit "Не любить!" %></div>
<% end %>

